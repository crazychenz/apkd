[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "thirdparty-python-repl"
version = "0.0.1"
description = "An async/socket based REPL for python"
requires-python = ">=3.7"

dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

#[project.scripts]
#mycli = "mypackage.cli:main"