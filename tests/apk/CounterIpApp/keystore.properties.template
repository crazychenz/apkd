# Copy this file's values from your real keystore, or overwrite this file
# directly. Do NOT commit the real version of this file to source control.
#
# storeFile is relative to the project root (where this file lives),
# or use an absolute path.

storeFile=../my-release-key.jks
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
